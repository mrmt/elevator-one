{
  "name": "elevator-one",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "generative ambient / minimal / drone",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
