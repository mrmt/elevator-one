# Changelog

## 未リリース

- **録音機能を廃止した** (Issue #34) — 使われていないのにメンテナンスコストがあり、
  ファイル書き出しという破壊的操作への経路を抱え、ヘッダーの1ボタン分の領域も占めていた。
  録音には代替手段 (オーディオアウトからの録音、BlackHole 2ch など) がある。
  `MediaRecorder` / `Blob` / `URL.createObjectURL` / `<a download>` の一式と ● ボタン、
  関連する i18n の6キー、録音中の色 `--oxide` をすべて削除。ヘッダーは ▶ / ⏸ の2つになった。
  `MediaStreamDestination` はバックグラウンド再生 (`<audio>` へ流す経路) で必要なので残している。
  状態で文言が変わる唯一の例外だった `updateRecLabel()` が消え、i18n は宣言的な流し込みだけになった

## v1.4

- **再生 / 一時停止ボタンからテキストを外し、3つのアイコンボタンの大きさを揃えた** (Issue #30) —
  ▶ / ⏸ にはラベルが付いていて、アイコンだけの ● と大きさが合っていなかった。
  3つとも同じ正方形のアイコンボタンにする。文言は `aria-label` とツールチップに残してあるので
  読み上げは変わらない。狭幅でラベルだけ畳んでいた分岐も不要になった
- **音量スライダー (0〜100) を追加し、全体の音量を上げられるようにした** — 出力が小さく、
  実測でスライダー最大でもピーク -9〜-12 dBFS / RMS -18〜-21 dBFS しかなかった。
  パラメータ節の先頭に 0〜100 の音量スライダーを置き、既存の「音量 / level」を統合した
  (0〜80 が従来の level 0〜1.0 に対応。既定 80 = 従来の最大)。80 を超えるとコンプを深く速くして
  メイクアップゲインとマスターを持ち上げ、最大で RMS -9 dBFS 前後まで音圧を稼ぐ。
  出力の最終段に tanh のソフトクリッパーを足し、持ち上げてもピークが 1.0 を超えないようにした。
  録音も新しい最終段から取るので、聞こえている音量のまま保存される
- **シャッフルの切り替え間隔をスライダーで変えられるようにした** — シャッフルボタンの下に
  10〜120秒のスライダーを置いた (既定は従来どおり30秒)。動かすと即座に次の切り替えへ
  反映され、他のスライダーと違ってシャッフルはOFFにならない
- **発音ボタンを再生 / 一時停止の2ボタンにした** (Issue #25) — 実質的に音楽再生アプリなのに、
  画面上は「発音 on/off」、iOS のロック画面は play / pause と意味がずれていた。ロック画面と
  同じく ▶ と ⏸ を並べ、いま置かれている状態の側を点灯させる。ボタン位置が状態で
  入れ替わらないので押し間違えない。LED と on/off 表示はアイコンと重複するので廃止。
  狭幅ではラベルを省いてアイコンだけにし、ヘッダーの1行を守る
- **録音ボタンを ● だけにし、言語切替をバージョンの隣に移した** (Issue #25) — 操作ボタンは
  ▶ / ⏸ / ● のアイコンに揃え、表示の切り替えでしかない `JA` / `EN` は枠を外して
  バージョン表示と同じ字面で隣に置く。録音中かどうかは色で示し、文言 (録音 / 停止・保存 /
  録音不可) は `aria-label` とツールチップに残した
- **中断されたら自力で再生に戻るようにした** — 割り込み (通知音・他アプリの再生・電話) で
  `AudioContext` が `interrupted` に落ちたり `<audio>` が pause させられたとき、復帰処理が
  `visibilitychange` にしか繋がっておらず、画面ロック中に止められるとそのまま戻らなかった。
  `ctx.onstatechange` / `<audio>` の pause・ended / 定期点検からも復帰を試みる。
  なお Issue #23 (iPadOS で約50分後に落ちる) の原因はこれではなかった。そちらは
  アプリ側で作るノードとパラメータ操作を完全にゼロにしても再現するため未解決で、
  既知の制約として `docs/ARCHITECTURE.md` に記録した
- **背景でも鳴り終わったノードを片付けるようにした** — 時計は AudioWorklet に
  移してあったのに、後片付けとステップ表示だけが `setTimeout` に残っていた。背景では
  これが絞られるため、鳴り終わったノードがグラフに繋がったまま増え続ける。
  `ctx.currentTime` 基準の予定に変えて音声時計から回す。併せて4拍ごとの低音パルスだけ
  切り離しが漏れていたのを修正した
- **狭幅時に XY パッドの見出し行を省いた** (Issue #7) — タブ表示中は選択中のタブ自体が
  「XYパッド」であることを示しているため、`XY PAD — DRAG TO MOVE THE TARGET` の行は冗長。
  そのぶんパッドの表示領域が広がる。デスクトップ幅では従来どおり見出しを出す
- **狭幅でもバージョンを表示するようにした** (Issue #8) — 狭幅では `header .sub` をまとめて
  隠していたためバージョンも消えていた。タグラインだけを省き、バージョンはタイトルの右に
  小さく残す。ヘッダーの高さは変わらない
- **横長のXYパッドではKPI表示を2列2行にした** (Issue #21) — iOS の landscape などパッドが
  横長になると、縦に積んだ4つの数値が上下に見切れていた。パッド自身をコンテナにして
  アスペクト比が2以上のときだけ2列2行に折り返す。2行に減った分、文字も少し大きく戻している
- **現在の mood をヘッダー / ブラウザのタイトル / ロック画面に出すようにした** (Issue #12) —
  シャッフル中は mood タブを開かないと何が鳴っているのか分からなかった。ヘッダーのバージョンの
  右、`document.title`、`MediaMetadata` (ロック画面・コントロールセンター・CarPlay) の3か所に
  同じ名前を出す。`MediaMetadata.artist` はアプリ名とバージョン (`Elevator One v1.3`)。
  スライダーやXYパッドで手を入れたあとは末尾に ` (edit)` が付く
- **ツールチップが5秒で自動的に消えるようにした** (Issue #11) — ポインタを動かさないと
  `mouseout` が来ないため、ヘルプが出しっぱなしになっていた。表示から5秒でフェードアウトする
  (自動消滅だけは0.9秒かけてゆっくり畳む)。消えたあともポインタを動かせば出し直せる
- **CI のログでテストの進捗が見えるようにした** (Issue #17) — `github` reporter は失敗時の注釈しか
  出さないため、実行中のテスト名が分からなかった。`list` reporter を併用して1テスト1行で流す。
  併せて `python3 -m http.server` のアクセスログ (stderr) を止め、`[WebServer]` の行で
  埋もれないようにした
- **英語表示の「展開」を `progression` に統一した** (Issue #10) — `section.harm` が
  `development` になっており音楽用語として違和感があった。ツールチップとプリセット説明で
  使っていた `development` も併せて `progression` に揃えた
- **iOS の landscape で上に余計な余白が出ないようにした** (Issue #9) — `viewport-fit=cover` を
  指定していなかったため、iOS がページ全体をセーフエリア内に押し込んでいた。cover を指定して
  背景を画面全体に広げ、ノッチ / ホームインジケータは `body` の `env(safe-area-inset-*)` で避ける

## v1.3

- **iOS でバックグラウンド / 画面ロック中も再生が続くようにした** (Issue #6) — iOS の Safari は
  Web Audio とタイマーの2つを止めにくるので、両方を塞いだ
  - 出力を `MediaStreamDestination` 経由で `<audio>` 要素に流し、ページを「メディア再生中」として
    扱わせる。実際に再生が進んだことを確認できたときだけ `ctx.destination` を切り離すので、
    対応していない環境では従来の経路のまま鳴る
  - `navigator.audioSession.type = 'playback'` の申告と `MediaSession` の登録を追加。
    ロック画面 / コントロールセンターに再生・一時停止のボタンが出る
  - スケジューリングの時計を `setInterval` から `AudioWorklet` に移した。バックグラウンドでの
    タイマー制限を受けないため、シーケンサと単発音が止まらない。使えない環境では従来の
    `setInterval` へフォールバックする
  - 単発音 / グレイン / 根音移動の `setTimeout` 連鎖を、`ctx.currentTime` 基準の予定時刻を
    突き合わせる方式に変更
- テストに `background-chromium` / `background-webkit` project を追加。発音時に上記の経路が
  組まれることを検証する (実機でのロック中再生は自動テストでは確かめられない)
- iPhone 実機で、画面ロック中の継続再生・バックグラウンドでの継続再生・ロック画面の操作ボタンを確認した。
  **Wi-Fi 経由の CarPlay での途切れは解消していない** — USB 接続では途切れないため、
  ワイヤレス通信スタックと映像エンコードによる端末側の負荷が原因で、本件の対処範囲外

## v1.2

- **シャッフルを既定でONに変更** — 開いた時点から雰囲気が30秒ごとに切り替わる。
  プリセットを選ぶ、スライダーやXYパッドを操作すると従来どおりOFFになる
- **日本語 / 英語の2言語に対応** (Issue #1) — UI文字列をすべてメッセージカタログ `I18N` に集め、
  直書きをやめた。ヘッダーの `JA` / `EN` ボタンでいつでも切り替えられる
- 初期言語はブラウザの言語設定で決まる (日本語なら ja、それ以外は en)。
  選択は `localStorage` に保存され、保存値があるときはそちらが優先される
- プリセットの定義を `{n, d}` から `{id}` に整理し、名前と説明はカタログ側へ移動。
  日本語表示のときの英語名 (`静止 / stillness` の2段表示) は従来どおり
- テストに `i18n-chromium` project を追加。ブラウザ言語による初期表示、切り替えと永続化、
  カタログの ja / en でキー集合が一致すること (訳し忘れ検知) を検証する
- 狭幅のレイアウト検証 (`mobile-webkit`) は英語で走らせるようにした。
  英語のほうが文字列が長く、ヘッダーが折り返す危険が高いため

## v1.1

- **展開（和声）を追加** — ドローン展開に加えて、マイナー調のコード進行を鳴らす「展開」系を実装。
  「和声率」でドローンと展開をクロスフェード。「刻み」でコードを持続音／リズムに切り替え、
  2小節または4小節ごとにポルタメントなしで和音が変わる
- プリセットを3種追加：大気 stratum / 均衡 equipoise / 失調 disequilibrium
- **ざらつき（旧・粒子）を実装し直し** — 単なるノイズ量ではなく、波形の歪み・リングモジュレーション・
  粉塵ノイズ・グレインを同時に立ち上げる質感チェーンに変更
- パラメータ名の整理：
  - 「共振」→ シーケンサ用に「カットオフ」と「レゾナンス」に分離（それぞれ独立制御に）
  - 「粒子」→「ざらつき」
  - 「シーケンス」（音量フェーダー）→「音量」
- シーケンサの4拍ごとの低音パルスが「音量」フェーダーを無視していたバグを修正
  （`dry` 直結 → `build.seq.bus` 経由に変更）
- **シャッフル機能を追加** — ONで30秒ごとに雰囲気をランダムに切り替え。切り替えは「推移時間」で
  クロスフェード。手動操作（プリセット選択／スライダー／XYパッド）で自動的にOFFになる
- **自動起動** — ページを開いた直後に起動を試行。ブラウザの自動再生ポリシーでブロックされた場合は
  最初の操作（クリック／キー入力）で起動する
- アプリ名を「Drone Engine」から「Elevator One」に変更
- UI改善：
  - XYパッドの縦軸を反転（上=疎、下=密）し、軸ラベルの上下逆さ表示を修正
  - パッド内の数値読み出しをKPI風の大型表示に変更、中央揃え・フォント変更
  - 背景色をXYパッドの現在地（明るさ×密度）から生成。海の深さのメタファー
    （疎＝青緑・明るい、密＝濃紺・暗い）。「ゆらぎ」で背景境界がゆっくり上下する「呼吸」を追加
  - マリンスノー（漂う粒子）を密度に応じて可変サイズに（疎＝大粒少数、密＝小粒多数）
  - 各種ラベルにホバー／フォーカスで説明が出るツールチップを追加
  - ウィンドウの高さ・幅に応じてレイアウトを圧縮し、XYパッドが見切れないよう調整
  - 推移時間の既定値を60秒→15秒に変更

## v1.0

- 初期リリース。ドローン展開（5声＋ノイズ層＋金属共鳴＋まばらなFMベル）と、
  16ステップのシーケンサ（ベルリン・スクール風、左右非対称の2系統ディレイ）を実装
- XYパッド（明るさ×密度）とプリセット12種、パラメータの目標値への滑らかな追従（推移時間）
- 録音機能（`MediaRecorder` による webm 書き出し）
