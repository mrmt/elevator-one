# Changelog

## v1.2

- **日本語 / 英語の2言語に対応** (Issue #1) — UI文字列をすべてメッセージカタログ `I18N` に集め、
  直書きをやめた。ヘッダーの `JA` / `EN` ボタンでいつでも切り替えられる
- 初期言語はブラウザの言語設定で決まる (日本語なら ja、それ以外は en)。
  選択は `localStorage` に保存され、保存値があるときはそちらが優先される
- プリセットの定義を `{n, d}` から `{id}` に整理し、名前と説明はカタログ側へ移動。
  日本語表示のときの英語名 (`静止 / stillness` の2段表示) は従来どおり
- テストに `i18n-chromium` project を追加。ブラウザ言語による初期表示、切り替えと永続化、
  カタログの ja / en でキー集合が一致すること (訳し忘れ検知) を検証する
- 狭幅のレイアウト検証 (`mobile-webkit`) は英語で走らせるようにした。
  英語のほうが文字列が長く、ヘッダーが折り返す危険が高いため

## v1.1

- **展開（和声）を追加** — ドローン展開に加えて、マイナー調のコード進行を鳴らす「展開」系を実装。
  「和声率」でドローンと展開をクロスフェード。「刻み」でコードを持続音／リズムに切り替え、
  2小節または4小節ごとにポルタメントなしで和音が変わる
- プリセットを3種追加：大気 stratum / 均衡 equipoise / 失調 disequilibrium
- **ざらつき（旧・粒子）を実装し直し** — 単なるノイズ量ではなく、波形の歪み・リングモジュレーション・
  粉塵ノイズ・グレインを同時に立ち上げる質感チェーンに変更
- パラメータ名の整理：
  - 「共振」→ シーケンサ用に「カットオフ」と「レゾナンス」に分離（それぞれ独立制御に）
  - 「粒子」→「ざらつき」
  - 「シーケンス」（音量フェーダー）→「音量」
- シーケンサの4拍ごとの低音パルスが「音量」フェーダーを無視していたバグを修正
  （`dry` 直結 → `build.seq.bus` 経由に変更）
- **シャッフル機能を追加** — ONで30秒ごとに雰囲気をランダムに切り替え。切り替えは「推移時間」で
  クロスフェード。手動操作（プリセット選択／スライダー／XYパッド）で自動的にOFFになる
- **自動起動** — ページを開いた直後に起動を試行。ブラウザの自動再生ポリシーでブロックされた場合は
  最初の操作（クリック／キー入力）で起動する
- アプリ名を「Drone Engine」から「Elevator One」に変更
- UI改善：
  - XYパッドの縦軸を反転（上=疎、下=密）し、軸ラベルの上下逆さ表示を修正
  - パッド内の数値読み出しをKPI風の大型表示に変更、中央揃え・フォント変更
  - 背景色をXYパッドの現在地（明るさ×密度）から生成。海の深さのメタファー
    （疎＝青緑・明るい、密＝濃紺・暗い）。「ゆらぎ」で背景境界がゆっくり上下する「呼吸」を追加
  - マリンスノー（漂う粒子）を密度に応じて可変サイズに（疎＝大粒少数、密＝小粒多数）
  - 各種ラベルにホバー／フォーカスで説明が出るツールチップを追加
  - ウィンドウの高さ・幅に応じてレイアウトを圧縮し、XYパッドが見切れないよう調整
  - 推移時間の既定値を60秒→15秒に変更

## v1.0

- 初期リリース。ドローン展開（5声＋ノイズ層＋金属共鳴＋まばらなFMベル）と、
  16ステップのシーケンサ（ベルリン・スクール風、左右非対称の2系統ディレイ）を実装
- XYパッド（明るさ×密度）とプリセット12種、パラメータの目標値への滑らかな追従（推移時間）
- 録音機能（`MediaRecorder` による webm 書き出し）
